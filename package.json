{
  "name": "spec-docs",
  "version": "1.0.0",
  "private": true,
  "description": "Bộ chuẩn tài liệu đặc tả (specification standard) cho dự án web/app",
  "scripts": {
    "format": "prettier --write \"**/*.md\"",
    "format:check": "prettier --check \"**/*.md\"",
    "lint": "markdownlint-cli2",
    "lint:fix": "markdownlint-cli2 --fix"
  },
  "devDependencies": {
    "markdownlint-cli2": "^0.22.1",
    "prettier": "^3.3.0"
  }
}
